#here direct delete method is not available so we have to do it by import module which is os module

# some are preinstalled but some arenot available in python which are tensorflow
# nd if we want to install any module then we have to write "pip install modulename" or "pip3 install modulename"

import os
# os.remove(filename)
os.remove("D:\Python_Codes\Python\Files_Operations\Delete.txt")